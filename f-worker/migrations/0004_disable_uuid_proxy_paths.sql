-- Disable nodes whose proxy_path resembles a UUID (SEC-14: path is public, not a credential)
UPDATE nodes
SET enabled = 0, updated_at = datetime('now')
WHERE proxy_path GLOB '????????-????-????-????-????????????'
   OR (length(proxy_path) = 32 AND proxy_path GLOB '[0-9a-fA-F]*');
